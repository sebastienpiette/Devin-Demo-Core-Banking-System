import { PlaceholderView } from '../components/PlaceholderView.tsx'

export function InitDatabasePage() {
  return (
    <PlaceholderView title="Init Database">
      Seeding the mock accounts (FR-4) is not implemented yet.
    </PlaceholderView>
  )
}
